# 🎓 Образовательная платформа от Mogjah
# Участники
1. Плаксеев Дмитрий - капитан (Frontend)
2. Добренков Владимир (Frontend)
3. Широков Максим (Backend)
4. Зыков Владимир (Backend)
5. Яковлев Владимир (Database Developer)

# Ссылка на гугл-диск с видеообзором рабочего веб-приложения
https://drive.google.com/file/d/1IGZ9XPoKyjwrBGlp7GylHoLW3j4IatW4/view?usp=drive_link](https://drive.google.com/file/d/1IGZ9XPoKyjwrBGlp7GylHoLW3j4IatW4/view?usp=drive_link)

## 📋 Оглавление
1. [Описание проекта](#описание-проекта)
2. [Технологический стек](#технологический-стек)
3. [Структура проекта](#структура-проекта)
4. [Предварительные требования](#предварительные-требования)
5. [Установка зависимостей](#установка-зависимостей)
6. [Настройка и запуск](#настройка-и-запуск)
7. [Инициализация данных](#инициализация-данных)
8. [Тестирование функционала](#тестирование-функционала)
9. [Администрирование](#администрирование)
10. [API endpoints](#api-endpoints)
11. [Устранение неполадок](#устранение-неполадок)

## 🎯 Описание проекта

Образовательная платформа - это полнофункциональная система дистанционного обучения, реализующая сценарии студентов и преподавателей. Платформа предоставляет возможности создания курсов, прохождения уроков, выполнения заданий и системы оценивания.

### Основной функционал:
- **Для студентов**: Регистрация, запись на курсы, прохождение уроков, сдача заданий, отслеживание прогресса
- **Для преподавателей**: Создание курсов и уроков, проверка работ, выставление оценок, система комментариев
- **Общее**: Адаптивный дизайн, система тем, поиск и фильтрация курсов, REST API

## 🛠 Технологический стек

### Backend:
- **Python 3.8+**
- **Django 5.2**
- **Django REST Framework**
- **SQLite** (разработка)

### Frontend:
- **HTML5, CSS3, JavaScript**
- **Bootstrap 5**
- **Font Awesome**

### Дополнительные компоненты:
- **Pillow** - работа с изображениями
- **django-cors-headers** - CORS настройки

## 📁 Структура проекта

```
education_platform_new/
│
├── 📄 Файлы управления:
│   ├── manage.py              # Утилита управления Django
│   ├── requirements.txt       # Список зависимостей Python
│   ├── README.md              # Документация проекта
│   ├── structure.txt          # Описание структуры проекта
│   └── db.sqlite3             # База данных SQLite
│
├── 🔧 Скрипты инициализации:
│   ├── create_groups.py       # Создание групп пользователей
│   ├── create_sample_data.py  # Создание тестовых данных
│   └── create_user_profiles.py # Создание профилей пользователей
│
├── 👥 Приложение accounts/:
│   ├── models.py              # Модели UserProfile
│   ├── views.py               # Аутентификация, регистрация, ЛК
│   ├── forms.py               # Формы пользователей
│   ├── urls.py                # Маршруты аутентификации
│   ├── admin.py               # Настройки админки для пользователей
│   ├── api_views.py           # API endpoints для аккаунтов
│   ├── api_urls.py            # API маршруты
│   ├── serializers.py         # Сериализаторы DRF
│   ├── middleware.py          # Промежуточное ПО
│   ├── utils.py               # Вспомогательные функции
│   └── migrations/            # Миграции базы данных
│
├── 📚 Приложение core/:
│   ├── models.py              # Модели: Course, Lesson, Assignment, etc.
│   ├── views.py               # Основная бизнес-логика
│   ├── urls.py                # Маршруты приложения
│   ├── admin.py               # Настройки админки
│   ├── admin_views.py         # Кастомные views для админки
│   ├── api_views.py           # API views (ViewSets)
│   ├── api_urls.py            # API маршруты
│   ├── serializers.py         # Сериализаторы DRF
│   ├── management/commands/   # Кастомные management commands
│   └── migrations/            # Миграции базы данных
│
├── ⚙️ Настройки проекта education_platform_new/:
│   ├── settings.py            # Основные настройки Django
│   ├── urls.py                # Корневые URL маршруты
│   ├── wsgi.py                # WSGI конфигурация
│   └── asgi.py                # ASGI конфигурация
│
├── 🎨 Статические файлы static/:
│   ├── css/                   # Стили CSS
│   │   ├── base.css           # Основные стили
│   │   ├── dark-theme.css     # Темная тема
│   │   └── themes/            # Дополнительные темы
│   └── js/
│       └── theme-switcher.js  # Переключатель тем
│
├── 📝 Шаблоны templates/:
│   ├── base.html              # Базовый шаблон
│   ├── index.html             # Главная страница
│   │
│   ├── admin/                 # Шаблоны админки
│   ├── assignments/           # Страницы заданий
│   ├── courses/               # Страницы курсов
│   ├── lessons/               # Страницы уроков
│   ├── profile/               # Личный кабинет
│   ├── registration/          # Аутентификация
│   └── teacher/               # Интерфейс преподавателя
│
└── 📁 Медиа файлы media/:
    └── profiles/              # Аватары пользователей
```

## 📋 Предварительные требования

### Системные требования:
- **ОС**: Windows 10/11, macOS 10.14+, Ubuntu 18.04+
- **Python**: версия 3.8 или выше
- **Память**: минимум 2GB свободной памяти
- **Дисковое пространство**: минимум 1GB

### Установка Python (если не установлен):

#### Windows:
1. Скачайте Python с [официального сайта](https://www.python.org/downloads/)
2. Запустите installer, отметьте "Add Python to PATH"
3. Установите, выбрав "Install Now"

#### macOS:
```bash
# Через Homebrew
brew install python

# Или скачайте с официального сайта
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip python3-venv
```

## 📦 Установка зависимостей

### Шаг 1: Создание виртуального окружения

#### Windows:
```bash
# Создание виртуального окружения
python -m venv venv

# Активация окружения
venv\Scripts\activate
```

#### macOS/Linux:
```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация окружения
source venv/bin/activate
```

### Шаг 2: Установка пакетов из requirements.txt

```bash
# Установка всех зависимостей
pip install -r requirements.txt
```

### Содержимое requirements.txt:
```
asgiref==3.11.0
Django==4.2.7
django-cors-headers==4.4.0
django-webpack-loader==2.0.0
djangorestframework==3.15.1
pillow==12.0.0
python-decouple==3.8
setuptools==80.9.0
sqlparse==0.5.4
tzdata==2025.2
wheel==0.45.1

```

### Шаг 3: Проверка установки
```bash
# Проверьте установленные пакеты
pip list

# Должны отображаться:
# Django, djangorestframework, Pillow, django-cors-headers
```

## ⚙️ Настройка и запуск

### Шаг 1: Применение миграций базы данных

```bash
# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate
```

### Шаг 2: Сборка статических файлов

```bash
# Сбор статических файлов
python manage.py collectstatic --noinput
```

### Шаг 3: Запуск сервера разработки

```bash
# Стандартный запуск
python manage.py runserver

# Сервер будет доступен по адресу:
# http://127.0.0.1:8000/

# Запуск на определенном порту
python manage.py runserver 8080

# Запуск для доступа из сети
python manage.py runserver 0.0.0.0:8000
```

## 🗃️ Инициализация данных

### Шаг 1: Создание групп пользователей

```bash
# Создание групп Teachers и Students
python create_groups.py
```

### Шаг 2: Создание тестовых данных

```bash
# Создание тестовых пользователей и курсов
python create_sample_data.py
```

### Шаг 3: Создание суперпользователя (опционально)

```bash
# Создание администратора
python manage.py createsuperuser

# Следуйте инструкциям в консоли
```

### Созданные тестовые пользователи:
- **Преподаватель**: `teacher1` / `password123`
- **Студент**: `student1` / `password123`

## 🧪 Тестирование функционала

### Сценарий 1: Тестирование роли студента

#### Шаг 1: Регистрация и вход
1. Перейдите на `http://127.0.0.1:8000/`
2. Нажмите "Регистрация" → создайте нового пользователя
3. Автоматически назначается роль "Student"

#### Шаг 2: Работа с курсами
1. Раздел "Курсы" - просмотр и поиск курсов
2. "Мои курсы" - управление записанными курсами
3. Запись на курс через детальную страницу курса

#### Шаг 3: Обучение и задания
1. Раздел "Уроки" - изучение материалов
2. Раздел "Задания" - сдача работ
3. "Личный кабинет" - отслеживание прогресса

### Сценарий 2: Тестирование роли преподавателя

#### Вход как преподаватель:
- Логин: `teacher1`
- Пароль: `password123`

#### Доступные функции:
1. **Панель преподавателя** (`/teacher/`) - дашборд со статистикой
2. **Мои курсы** (`/teacher/courses/`) - управление курсами
3. **Проверка работ** (`/teacher/submissions/`) - оценка студенческих работ
4. **Админ-панель** (`/admin/`) - полное управление контентом

#### Создание учебного контента:
1. Через админ-панель создайте Курсы → Уроки → Задания
2. Или используйте быстрые ссылки в интерфейсе преподавателя

### Сценарий 3: Проверка работ преподавателем

1. Перейдите в "Проверка работ"
2. Выберите работу студента
3. Нажмите "Оценить"
4. Установите баллы и комментарий
5. Сохраните оценку

## 🔐 Администрирование

### Доступ к админ-панели:
```bash
http://127.0.0.1:8000/admin/
```

### Управление через админку:
- **Пользователи и группы** - управление правами
- **Курсы** - создание и редактирование учебных программ
- **Уроки** - управление образовательным контентом
- **Задания** - настройка учебных задач
- **Оценки и комментарии** - просмотр системы оценивания

### Создание дополнительных преподавателей:
```bash
python manage.py shell
```

```python
from django.contrib.auth.models import User, Group

user = User.objects.create_user('new_teacher', 'teacher@example.com', 'password123')
teachers_group = Group.objects.get(name='Teachers')
user.groups.add(teachers_group)
user.is_staff = True  # Для доступа к админке
user.save()
```

## 🌐 API Endpoints

### Аутентификация:
- `POST /accounts/register/` - регистрация пользователя
- `POST /accounts/login/` - вход в систему
- `POST /accounts/logout/` - выход из системы

### API endpoints:
- `GET /api/core/courses/` - список курсов
- `GET /api/core/courses/{id}/` - детали курса
- `GET /api/core/lessons/` - список уроков
- `GET /api/core/assignments/` - список заданий
- `POST /api/core/submissions/` - отправка работы
- `POST /api/core/submissions/{id}/grade/` - оценка работы

### Пример использования API:
```bash
# Получение списка курсов (требуется аутентификация)
curl -X GET http://127.0.0.1:8000/api/core/courses/
```

## 🐛 Устранение неполадок

### Частые проблемы и решения:

#### 1. Ошибка "Port already in use":
```bash
# Найдите процесс использующий порт (Windows)
netstat -ano | findstr :8000

# Используйте другой порт
python manage.py runserver 8001
```

#### 2. Ошибки миграций:
```bash
# Сброс и повторное применение миграций
python manage.py makemigrations
python manage.py migrate

# Если проблемы сохраняются
rm db.sqlite3
python manage.py migrate
```

#### 3. Проблемы со статическими файлами:
```bash
# Принудительный сбор статических файлов
python manage.py collectstatic --noinput

# Запуск в insecure режиме для разработки
python manage.py runserver --insecure
```

#### 4. Ошибки импорта модулей:
```bash
# Проверка установки зависимостей
pip list | grep -E "(Django|rest|Pillow)"

# Переустановка зависимостей
pip install -r requirements.txt --force-reinstall
```

#### 5. Проблемы с виртуальным окружением:
```bash
# Пересоздание виртуального окружения
deactivate
rm -rf venv
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

#### 6. Ошибки доступа к медиа файлам:
- Убедитесь что папка `media/` существует
- Проверьте права доступа к папке
- В режиме разработки Django обслуживает медиа файлы автоматически

### Проверка работоспособности:

1. **Сервер запускается без ошибок**
2. **Главная страница доступна** (`http://127.0.0.1:8000/`)
3. **Админ-панель доступна** (`http://127.0.0.1:8000/admin/`)
4. **Статические файлы загружаются** (стили, скрипты)
5. **Регистрация и вход работают**
6. **Создание и отображение курсов работает**

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи Django** в консоли запуска
2. **Убедитесь в правильности установки** всех зависимостей
3. **Проверьте применены ли миграции** (`python manage.py showmigrations`)
4. **Убедитесь что база данных существует** и доступна для записи

Для дополнительной помощи:
- Обратитесь к документации Django
- Проверьте настройки в `education_platform_new/settings.py`
- Убедитесь что все необходимые папки существуют

---

**🎉 Поздравляем! Вы успешно настроили и запустили образовательную платформу. Приятного тестирования!**

**Доступные тестовые аккаунты:**
- Преподаватель: `teacher1` / `password123`
- Студент: `student1` / `password123`
